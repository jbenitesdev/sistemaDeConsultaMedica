import { all } from 'redux-saga/effects';
// import { escritoriosSaga } from '../modules/Escritorio/saga/escritorioSaga';

export function* mainSaga() {
    yield all([
        // aceitarConviteSaga(),
    ]);
}
